# yaml-language-server: $schema=https://json.schemastore.org/any.json
spring:
  application:
    name: rag-assistant
  ai:
    openai:
      chat:
        api-key: ${OPENROUTER_API_KEY}
        base-url: https://openrouter.ai/api
        options:
          model: gpt-4o-mini
      embedding:
        api-key: ${OPENAI_API_KEY}
        base-url: https://api.openai.com
        options:
          model: text-embedding-3-small
    vectorstore:
      qdrant:
        host: ${QDRANT_HOST:localhost}
        port: 6334
        collection-name: rag-docs
        use-tls: false
  servlet:
    multipart:
      # SonarLint java:S5693: Limit set to 8MB to comply with default security guidelines while providing
      # ample headroom for multi-page text PDFs, word files, and markdown archives.
      max-file-size: 8MB
      max-request-size: 8MB
server:
  port: 8080
jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: 86400000
